<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE rfc [
  <!ENTITY nbsp "&#160;">
  <!ENTITY zwsp "&#8203;">
  <!ENTITY nbhy "&#8209;">
  <!ENTITY wj   "&#8288;">
]>
<?xml-stylesheet type="text/xsl" href="rfc2629.xslt"?>

<rfc xmlns:xi="http://www.w3.org/2001/XInclude"
     category="info"
     docName="draft-calabria-bmwg-ai-fabric-terminology-00"
     ipr="trust200902"
     obsoletes=""
     updates=""
     submissionType="IETF"
     xml:lang="en"
     tocInclude="true"
     tocDepth="3"
     symRefs="true"
     sortRefs="true"
     version="3">

  <!-- ============================================================ -->
  <!--  FRONT MATTER                                                 -->
  <!-- ============================================================ -->
  <front>
    <title abbrev="AI Fabric Benchmarking Terminology">
      Benchmarking Terminology for AI Network Fabrics
    </title>

    <seriesInfo name="Internet-Draft"
                value="draft-calabria-bmwg-ai-fabric-terminology-00"/>

    <author fullname="Fernando Calabria" initials="F." surname="Calabria">
      <organization>Cisco</organization>
      <address>
        <email>fcalabri@cisco.com</email>
      </address>
    </author>

    <author fullname="Carlos Pignataro" initials="C." surname="Pignataro">
      <organization>Blue Fern Consulting</organization>
      <address>
        <email>carlos@bluefern.consulting</email>
      </address>
    </author>

    <date month="February" year="2026"/>

    <area>Operations and Management</area>
    <workgroup>BMWG Working Group</workgroup>

    <keyword>benchmarking</keyword>
    <keyword>terminology</keyword>
    <keyword>AI training</keyword>
    <keyword>AI inference</keyword>
    <keyword>network fabric</keyword>
    <keyword>RDMA</keyword>
    <keyword>RoCEv2</keyword>
    <keyword>UET</keyword>
    <keyword>collective communication</keyword>
    <keyword>AllReduce</keyword>
    <keyword>JCT</keyword>
    <keyword>TTFT</keyword>
    <keyword>KV cache</keyword>

    <abstract>
      <t>
        This document defines benchmarking terminology for evaluating
        Ethernet-based network fabrics used in distributed Artificial
        Intelligence (AI) training and inference workloads. It provides a
        unified vocabulary consolidating and extending terms from RFC 1242,
        RFC 8238, and the companion AI fabric methodology documents,
        establishing precise, vendor-neutral definitions for collective
        communication primitives, RDMA transport mechanisms (RoCEv2 and Ultra
        Ethernet Transport), congestion control behaviors, AI-specific Key
        Performance Indicators (KPIs), and fabric topology concepts.
      </t>
      <t>
        This document is a companion to draft-bmwg-ai-fabric-training-bench-00
        and draft-bmwg-ai-fabric-inference-bench-00. Those documents
        <bcp14>SHOULD NOT</bcp14> be applied without first consulting the
        terminology defined herein. Where definitions herein overlap with
        RFC&nbsp;1242 or RFC&nbsp;8238, the AI fabric context definition in
        this document takes precedence.
      </t>
    </abstract>
  </front>

  <!-- ============================================================ -->
  <!--  MIDDLE MATTER                                                -->
  <!-- ============================================================ -->
  <middle>

    <!-- ── Section 1: Introduction ─────────────────────────────── -->
    <section anchor="intro" numbered="true" toc="default">
      <name>Introduction</name>

      <section anchor="req-lang" numbered="true" toc="default">
        <name>Requirements Language</name>
        <t>
          The key words <bcp14>MUST</bcp14>, <bcp14>MUST NOT</bcp14>,
          <bcp14>REQUIRED</bcp14>, <bcp14>SHALL</bcp14>, <bcp14>SHALL
          NOT</bcp14>, <bcp14>SHOULD</bcp14>, <bcp14>SHOULD NOT</bcp14>,
          <bcp14>RECOMMENDED</bcp14>, <bcp14>NOT RECOMMENDED</bcp14>,
          <bcp14>MAY</bcp14>, and <bcp14>OPTIONAL</bcp14> in this document are
          to be interpreted as described in BCP&nbsp;14 <xref target="RFC2119"/>
          <xref target="RFC8174"/> when, and only when, they appear in all
          capitals, as shown here.
        </t>
      </section>

      <section anchor="scope" numbered="true" toc="default">
        <name>Scope and Purpose</name>
        <t>
          This document defines terminology specifically for benchmarking
          Ethernet-based AI network fabrics in controlled laboratory
          environments as specified by the BMWG charter
          <xref target="charter-ietf-bmwg"/>. The defined terms cover:
          distributed AI training collective communication patterns, LLM
          inference serving architectures, RDMA transport semantics (RoCEv2
          and UET), congestion control mechanisms, fabric topology
          characteristics, and performance metric definitions.
        </t>
        <t>
          This document does not define acceptance criteria, performance
          requirements, or configuration recommendations. It does not address
          benchmarking of live operational networks, intra-node (NVLink/PCIe)
          interconnects, or storage networking.
        </t>
      </section>

      <section anchor="existing-bmwg" numbered="true" toc="default">
        <name>Relationship to Existing BMWG Work</name>
        <t>
          This document extends the foundational BMWG terminology established
          in <xref target="RFC1242"/> (network interconnect benchmarking
          terminology) and <xref target="RFC8238"/> (data center benchmarking
          terminology). Where terms are defined in those RFCs, this document
          provides AI fabric context extensions; the core definitions remain as
          established. This document also extends the test methodology framework
          of <xref target="RFC2544"/> and <xref target="RFC8239"/> as applied
          in the companion AI fabric methodology documents.
        </t>
      </section>

      <section anchor="companion-docs" numbered="true" toc="default">
        <name>Relationship to Companion Documents</name>
        <t>
          This document is one of three companion Internet-Drafts addressing AI
          fabric benchmarking:
        </t>
        <ul spacing="normal">
          <li>
            draft-bmwg-ai-fabric-terminology-00 (this document): Terminology
            definitions.
          </li>
          <li>
            <xref target="TRAINING-BENCH-00"/>: Benchmarking methodology for AI
            training workloads.
          </li>
          <li>
            <xref target="INFERENCE-BENCH-00"/>: Benchmarking methodology for
            AI inference serving workloads.
          </li>
        </ul>
        <t>
          Implementers and evaluators <bcp14>SHOULD</bcp14> read this
          terminology document before applying the companion methodology
          documents. Terms defined here are used normatively in those documents
          and are not redefined there unless the specific workload context
          introduces a substantive difference, which is noted explicitly.
        </t>
      </section>
    </section>

    <!-- ── Section 2: General Benchmarking Terms ────────────────── -->
    <section anchor="general-terms" numbered="true" toc="default">
      <name>General Benchmarking Terms</name>
      <t>
        The following terms establish the general measurement framework
        applicable to all AI fabric benchmarking activities.
      </t>
      <dl newline="true" spacing="normal">

        <dt>AI Fabric</dt>
        <dd>The dedicated Ethernet backend network interconnecting
        accelerators (GPUs/XPUs) for distributed AI training and inference
        workloads. Typically implemented as a non-blocking Clos (fat-tree)
        topology running RoCEv2 or UET transport. Distinct from the front-end
        (management/storage) network.</dd>

        <dt>DUT</dt>
        <dd>Device Under Test. The network element(s) whose performance
        characteristics are being measured. In AI fabric benchmarking the DUT
        is one or more fabric elements: leaf switches, spine switches, NICs, or
        the complete fabric assembly.</dd>

        <dt>SUT</dt>
        <dd>System Under Test. The complete AI compute system including
        accelerators, NICs, the fabric DUT, and serving/training software, when
        end-to-end metrics are the measurement objective.</dd>

        <dt>RT</dt>
        <dd>Router Tester / Traffic Generator. Test equipment capable of
        generating and receiving network traffic at specified rates with
        nanosecond-resolution timestamping sufficient for the measurements
        defined in the companion methodology documents.</dd>

        <dt>JFI</dt>
        <dd>Jain's Fairness Index. A scalar measure of flow-level throughput
        fairness across a set of n flows, defined as:
        <artwork align="center" type="ascii-art"><![CDATA[
JFI = (Σxᵢ)² / (n · Σxᵢ²)
        ]]></artwork>
        where xᵢ is the throughput of flow i. A value of 1.0 indicates perfect
        fairness; lower values indicate disparity. <bcp14>SHOULD</bcp14> be
        computed per <xref target="RFC1242"/> reporting conventions.</dd>

        <dt>Offered Load</dt>
        <dd>The total traffic rate presented to the DUT from test equipment,
        expressed as a fraction of line rate (0&ndash;100%) or as absolute
        bit/s. Offered load is controlled independently of DUT absorption,
        enabling characterization of saturation behavior.</dd>

        <dt>Trial Duration</dt>
        <dd>The time interval over which a single measurement is conducted.
        For AI fabric tests, the <bcp14>RECOMMENDED</bcp14> minimum trial
        duration is 60 seconds for throughput tests and 300 seconds for
        soak/stability tests, per the methodology in <xref target="RFC2544"/>
        as extended herein.</dd>

        <dt>Warmup Period</dt>
        <dd>A mandatory pre-measurement interval during which traffic is sent
        but results are not recorded. Ensures adaptive routing tables, PFC
        watermarks, and DCQCN/UET congestion controllers reach steady state
        before measurement begins. <bcp14>RECOMMENDED</bcp14> minimum:
        10 seconds.</dd>

        <dt>Binary Search</dt>
        <dd>An iterative test procedure for determining the maximum offered
        load at which a DUT meets a specified acceptance criterion (e.g., zero
        packet loss). The search halves the candidate load range at each
        iteration, converging to a resolution of 0.1% offered load within
        10 iterations.</dd>

        <dt>Percentile Latency</dt>
        <dd>A latency statistic expressing that the specified fraction of all
        measured latency samples fall at or below the reported value. Denoted
        Pxx (e.g., P50, P95, P99, P99.9). Tail latency (P99 and above) is
        especially relevant for AI fabric benchmarking because SLO violations
        are determined by worst-case, not median, performance.</dd>

      </dl>
    </section>

    <!-- ── Section 3: Collective Communication Terms ─────────────── -->
    <section anchor="collective-terms" numbered="true" toc="default">
      <name>Collective Communication Terms</name>
      <t>
        The following terms define the collective communication operations that
        are the primary traffic sources in distributed AI workloads.
      </t>
      <dl newline="true" spacing="normal">

        <dt>Collective Operation</dt>
        <dd>A coordinated communication pattern executed simultaneously across
        all accelerators in a training or inference group. Core collectives:
        AllReduce (gradient aggregation), AllGather (parameter distribution),
        ReduceScatter (partial reduction followed by scatter), and AllToAll
        (expert dispatch in MoE models).</dd>

        <dt>AllReduce</dt>
        <dd>A collective operation in which each participant contributes a
        tensor and all participants receive the element-wise sum (or other
        reduction) of all contributions. The dominant communication primitive
        in data-parallel and tensor-parallel training. Bus bandwidth (BusBW)
        is the primary KPI.</dd>

        <dt>AllGather</dt>
        <dd>A collective operation in which each participant contributes a
        shard of a tensor and all participants receive the concatenation of all
        shards. Used in tensor-parallel (Megatron-style) layers to reconstruct
        distributed activations or parameters.</dd>

        <dt>ReduceScatter</dt>
        <dd>A collective operation combining an element-wise reduction with a
        scatter, so each participant receives a distinct slice of the reduced
        result. Used in ZeRO-stage optimizer strategies and as the first half
        of a ring-AllReduce.</dd>

        <dt>AllToAll</dt>
        <dd>A collective operation in which each participant sends a distinct
        payload to every other participant and receives a distinct payload from
        every other participant. The critical collective for Mixture-of-Experts
        token dispatch. Generates N&sup2;&minus;1 independent
        point-to-point flows for N participants.</dd>

        <dt>Ring Algorithm</dt>
        <dd>An AllReduce (or AllGather/ReduceScatter) algorithm structured as a
        logical ring of participants. Each participant sends data to its right
        neighbor and receives from its left neighbor in 2(N&minus;1) steps.
        Bus bandwidth efficiency approaches 2(N&minus;1)/N, approaching 100%
        for large N. Standard baseline for BusBW calculation.</dd>

        <dt>BusBW</dt>
        <dd>Bus Bandwidth. The effective data throughput achieved per
        accelerator during a collective operation, normalizing for algorithm
        overhead:
        <artwork align="center" type="ascii-art"><![CDATA[
BusBW = (data_size × algo_factor) / elapsed_time
        ]]></artwork>
        For ring AllReduce, algo_factor = 2(N&minus;1)/N. BusBW enables
        comparison across cluster sizes and collective algorithms.</dd>

        <dt>CCL</dt>
        <dd>Collective Communication Library. A software library providing
        optimized implementations of collective operations (AllReduce,
        AllGather, etc.) over a specific transport (RDMA, shared memory).
        In benchmarking, the CCL implementation <bcp14>MUST</bcp14> be
        documented in the test report.</dd>

        <dt>SPMD</dt>
        <dd>Single Program Multiple Data. The execution model underlying
        bulk-synchronous distributed training, in which all accelerators
        execute identical computation on distinct data partitions,
        synchronizing at collective barriers between steps.</dd>

        <dt>Bulk Synchronous Parallel (BSP)</dt>
        <dd>A distributed computation model structured as alternating compute
        and communicate phases, with a global synchronization barrier between
        phases. Standard training workloads follow BSP: forward pass &rarr;
        backward pass &rarr; AllReduce gradient sync &rarr; optimizer
        step.</dd>

      </dl>
    </section>

    <!-- ── Section 4: Distributed Parallelism Terms ───────────────── -->
    <section anchor="parallelism-terms" numbered="true" toc="default">
      <name>Distributed Parallelism Strategy Terms</name>
      <t>
        The following terms define the parallelism strategies used in
        distributed AI model training and inference, which determine traffic
        patterns and fabric requirements.
      </t>
      <dl newline="true" spacing="normal">

        <dt>Data Parallelism (DP)</dt>
        <dd>A distributed training strategy replicating the full model on each
        accelerator, partitioning the training dataset across replicas. Gradient
        synchronization after each backward pass requires an AllReduce across
        all DP ranks. Memory-efficient for small models; communication overhead
        scales with parameter count.</dd>

        <dt>Tensor Parallelism (TP)</dt>
        <dd>A distributed training and inference strategy partitioning individual
        weight matrices across multiple accelerators (ranks). Each rank computes
        a partial result; AllGather or ReduceScatter collectives are required
        within each layer to aggregate results. Dominant parallelism within a
        node (intra-node).</dd>

        <dt>Pipeline Parallelism (PP)</dt>
        <dd>A distributed strategy assigning contiguous groups of transformer
        layers to distinct stages (accelerators or nodes). Each stage processes
        one microbatch and forwards activations to the next stage. Generates
        point-to-point inter-stage traffic across the fabric (activations and
        gradients).</dd>

        <dt>Expert Parallelism (EP)</dt>
        <dd>A parallelism strategy for Mixture-of-Experts models distributing
        expert sub-networks across accelerators. Each token is routed to its
        designated experts (typically top-K of E total experts), requiring
        AllToAll communication for dispatch. Wide EP (e.g., 96-way) generates
        dense inter-node AllToAll at every MoE layer.</dd>

        <dt>MoE</dt>
        <dd>Mixture of Experts. A transformer architecture replacing dense
        feed-forward layers with a set of E expert sub-networks, of which only
        top-K experts (typically K=2 or K=4) are activated per token via a
        learned router. MoE enables large model capacity with sub-linear
        compute, but introduces AllToAll communication requirements proportional
        to E and sequence length.</dd>

        <dt>DP Attention</dt>
        <dd>Data Parallelism applied to the attention computation, where the KV
        cache is partitioned across data-parallel ranks. Each rank holds
        1/DP_SIZE of the KV cache; AllToAll communication exchanges attention
        outputs. Used in inference to reduce per-accelerator memory footprint
        for long contexts.</dd>

        <dt>ZeRO</dt>
        <dd>Zero Redundancy Optimizer. A memory optimization strategy for
        data-parallel training that shards model states (parameters, gradients,
        optimizer states) across DP ranks instead of replicating them. ZeRO
        Stage&nbsp;1 shards optimizer states; Stage&nbsp;2 adds gradient
        sharding; Stage&nbsp;3 adds parameter sharding. Each stage increases
        AllGather/ReduceScatter communication.</dd>

      </dl>
    </section>

    <!-- ── Section 5: Network Transport Terms ────────────────────── -->
    <section anchor="transport-terms" numbered="true" toc="default">
      <name>Network Transport Terms</name>

      <section anchor="rocev2-terms" numbered="true" toc="default">
        <name>RoCEv2 and RDMA Terms</name>
        <t>
          The following terms define RDMA and RoCEv2 transport semantics as
          used in AI fabric benchmarking.
        </t>
        <dl newline="true" spacing="normal">

          <dt>RDMA</dt>
          <dd>Remote Direct Memory Access. A transport mechanism enabling
          direct memory-to-memory data transfer between hosts without involving
          the destination CPU, providing zero-copy semantics and kernel bypass.
          Implementations include InfiniBand Verbs (native IB), iWARP (RDMA
          over TCP), and RoCEv2 (RDMA over Converged Ethernet v2).</dd>

          <dt>RoCEv2</dt>
          <dd>RDMA over Converged Ethernet version 2. An RDMA transport
          encapsulating InfiniBand transport layer (BTH) over UDP/IP, enabling
          RDMA semantics on standard Ethernet infrastructure. Requires lossless
          fabric operation (PFC or equivalent) for correctness. Standardized in
          IBTA Annex 16 <xref target="IBTA-ROCE"/> and transported over UDP
          destination port 4791.</dd>

          <dt>QP</dt>
          <dd>Queue Pair. The fundamental RDMA communication endpoint comprising
          a Send Queue (SQ) and Receive Queue (RQ). QPs are
          connection-oriented in Reliable Connected (RC) mode. Multiple QPs per
          source-destination pair are used to increase ECMP entropy in fabric
          load balancing.</dd>

          <dt>Reliable Connected (RC)</dt>
          <dd>An RDMA QP transport service type providing reliable, in-order
          delivery between exactly two endpoints. The primary QP type for AI
          collective operations via RoCEv2. Requires connection setup before
          data transfer and maintains per-QP state for retransmission.</dd>

          <dt>RDMA Verb</dt>
          <dd>An operation primitive of the RDMA programming model. Key verbs:
          SEND/RECV (two-sided, receiver must post a buffer), WRITE (one-sided,
          target memory written directly), READ (one-sided, remote memory
          read), and Atomic (compare-and-swap, fetch-and-add). AI collectives
          predominantly use WRITE and SEND.</dd>

          <dt>UET</dt>
          <dd>Ultra Ethernet Transport. A transport protocol defined by the
          Ultra Ethernet Consortium (UEC) Specification 1.0
          <xref target="UEC-SPEC-1.0"/> as a next-generation AI/HPC fabric
          transport. UET is connectionless, supports native packet spraying
          (Reliable Unordered Delivery), and integrates multipath load
          balancing and congestion control. Transported over UDP destination
          port 4793 (pending IANA verification).</dd>

          <dt>PDC</dt>
          <dd>Packet Delivery Context. The ephemeral, lightweight transport
          endpoint in UET, analogous to but distinct from an RDMA Queue Pair.
          PDCs are connectionless (no setup handshake), enabling low-latency
          initiation and reduced per-flow state in the NIC and switch.</dd>

          <dt>ROD</dt>
          <dd>Reliable Ordered Delivery. A UET transport service providing
          reliable, in-order packet delivery, semantically equivalent to
          RoCEv2 RC mode. Suitable for legacy RDMA applications requiring
          strict ordering guarantees.</dd>

        </dl>
      </section>

      <section anchor="uet-terms" numbered="true" toc="default">
        <name>Ultra Ethernet Transport (UET) Terms</name>
        <t>
          The following terms define UET-specific concepts introduced by the
          Ultra Ethernet Consortium (UEC) Specification 1.0
          <xref target="UEC-SPEC-1.0"/>.
        </t>
        <dl newline="true" spacing="normal">

          <dt>RUD</dt>
          <dd>Reliable Unordered Delivery. A UET transport service providing
          reliable delivery without maintaining packet order across paths.
          Enables native packet spraying across ECMP paths without
          reorder-buffer overhead at the receiver NIC. The preferred UET
          service class for AI training collectives.</dd>

          <dt>RUDI</dt>
          <dd>Reliable Unordered Delivery for Idempotent operations. A UET
          transport service optimized for operations safe to execute more than
          once (e.g., RDMA Writes to non-accumulating targets), allowing
          simplified retransmission logic with reduced state overhead.</dd>

          <dt>UUD</dt>
          <dd>Unreliable Unordered Delivery. A UET transport service providing
          best-effort, unordered packet delivery with minimal overhead.
          Suitable for telemetry, speculative operations, or workloads with
          application-layer loss tolerance.</dd>

          <dt>UEC Profile</dt>
          <dd>A defined subset of UET features targeting a specific use case:
          AI Base (core AI training/inference, mandatory feature set), AI Full
          (AI Base plus deferred send, exact-match tagging, extended atomics),
          or HPC (latency-optimized for traditional HPC workloads with
          fine-grained synchronization).</dd>

          <dt>LLR</dt>
          <dd>Link Layer Retry. An optional UEC link-layer enhancement
          providing fast per-hop error recovery at the Ethernet link layer.
          LLR detects symbol errors at the FEC level and retransmits the
          affected frame before it is dropped, reducing the frequency of
          transport-layer retransmission and improving tail latency.</dd>

          <dt>Packet Trimming</dt>
          <dd>An optional UEC link-layer behavior in which a congested switch,
          rather than dropping the full packet, transmits only the packet
          header (trimmed packet) to the receiver. Trimming enables the
          receiver to detect loss and initiate selective retransmission more
          rapidly, reducing bandwidth waste versus silent drop.</dd>

          <dt>CBFC</dt>
          <dd>Credit-Based Flow Control. An optional UEC link-layer buffer
          management mechanism using explicit credit grants from downstream to
          upstream devices. CBFC provides backpressure without transmitting
          PFC PAUSE frames, eliminating the head-of-line blocking and storm
          propagation risks associated with PFC.</dd>

          <dt>Entropy Value</dt>
          <dd>A per-packet field in the UET header used to distribute packets
          of a single message across available ECMP paths, providing explicit
          spray entropy independent of the IP 5-tuple. Enables
          hardware-assisted packet spraying without requiring
          transport-layer state in the switch.</dd>

          <dt>GIN</dt>
          <dd>GPU-Initiated Networking. A communication paradigm in which GPU
          threads directly initiate network RDMA operations (sends, one-sided
          writes/reads) to the NIC hardware without CPU involvement, eliminating
          the CPU-GPU synchronization round-trip. Reduces effective latency by
          several microseconds for fine-grained operations.</dd>

          <dt>KVCXL</dt>
          <dd>KV Cache Transfer Library. A software library providing
          standardized point-to-point data transfer primitives (register,
          transfer, notify) for inference engines, abstracting underlying
          transport mechanisms (intra-node interconnect, RDMA, PCIe, storage
          interfaces). Enables transport-agnostic KV cache migration in
          disaggregated serving architectures.</dd>

        </dl>
      </section>
    </section>

    <!-- ── Section 6: Congestion Control Terms ───────────────────── -->
    <section anchor="congestion-terms" numbered="true" toc="default">
      <name>Congestion Control and Fabric Behavior Terms</name>
      <t>
        The following terms define congestion management mechanisms and
        associated fabric behaviors critical to AI workload performance.
      </t>
      <dl newline="true" spacing="normal">

        <dt>PFC</dt>
        <dd>Priority Flow Control (IEEE 802.1Qbb). A lossless Ethernet
        mechanism in which a receiver transmits a PAUSE frame to its upstream
        neighbor on a specific priority class when its ingress buffer approaches
        a configured threshold, temporarily halting transmission of that
        priority. Required for lossless RoCEv2 operation. PFC operates
        hop-by-hop and can propagate congestion upstream (PFC storm risk).</dd>

        <dt>PFC Storm</dt>
        <dd>A pathological condition in which PFC PAUSE frames propagate across
        multiple hops, causing widespread throughput degradation or deadlock
        unrelated to the original congestion source. Detection and mitigation
        <bcp14>SHOULD</bcp14> be part of soak test evaluation per the companion
        methodology documents.</dd>

        <dt>PFC Deadlock</dt>
        <dd>A circular PFC dependency in which sets of flows mutually pause each
        other indefinitely, resulting in zero progress for affected traffic
        classes. Deadlock risk is elevated in non-tree topologies (e.g., those
        with fabric loops under failure convergence) and <bcp14>MUST</bcp14> be
        evaluated in fabric-level soak tests.</dd>

        <dt>ECN</dt>
        <dd>Explicit Congestion Notification (<xref target="RFC3168"/>). An
        IP-layer mechanism in which a congested router marks packets with the
        Congestion Experienced (CE) codepoint in the IP ECN field instead of
        dropping them. The receiver echoes congestion feedback to the sender
        via the transport protocol, triggering rate reduction. Used with
        RoCEv2 as part of DCQCN.</dd>

        <dt>DCQCN</dt>
        <dd>Data Center Quantized Congestion Notification. An end-to-end
        congestion control algorithm for RoCEv2 flows, combining ECN marking
        at congested switches with rate-based sender reduction using an
        additive-increase/multiplicative-decrease (AIMD) scheme.
        Note: PFC serves as a separate, orthogonal backstop to prevent packet
        loss during DCQCN convergence; PFC is not a component of the DCQCN
        algorithm itself.</dd>

        <dt>ECN Marking Ratio</dt>
        <dd>The fraction of packets (expressed as a percentage) that are marked
        with the CE codepoint in the IP ECN field over a measurement interval.
        A high ECN Marking Ratio indicates persistent congestion and is a
        primary Fabric Health Indicator in AI fabric benchmarking.</dd>

        <dt>Incast</dt>
        <dd>A traffic pattern in which multiple sources simultaneously send to a
        single destination, potentially overwhelming the destination's NIC
        receive buffer and the switch's egress port buffer. Incast is a
        dominant congestion mechanism in AllReduce and collective operations,
        and a primary test scenario in the companion methodology documents.</dd>

        <dt>Incast Ratio</dt>
        <dd>The ratio of concurrent senders to receivers in an incast
        communication pattern (N:1). The incast ratio determines the
        oversubscription factor at the destination port and is a primary test
        parameter for congestion characterization.</dd>

        <dt>Packet Spray</dt>
        <dd>A load balancing strategy distributing individual packets of a
        single RDMA message across all available ECMP paths, maximizing link
        utilization at the cost of potential out-of-order delivery at the
        receiver. Native in UET (RUD mode); requires NIC reorder buffering for
        RoCEv2 RC mode.</dd>

        <dt>DLB / Flowlet</dt>
        <dd>Dynamic Load Balancing using flowlet detection. A per-flow
        rerouting mechanism that reassigns a flow to a new ECMP path when the
        flow has been idle longer than the flowlet gap threshold (typically
        500&nbsp;ns&ndash;2&nbsp;&micro;s), reducing out-of-order packet risk
        compared to packet spray while improving utilization over static
        per-flow ECMP.</dd>

        <dt>ECMP</dt>
        <dd>Equal-Cost Multi-Path routing. A forwarding mechanism distributing
        traffic across multiple equal-cost paths, typically via hash of the IP
        5-tuple (or entropy field in UET). ECMP imbalance (MMR &gt; 1.0) is a
        primary fabric efficiency metric for AI traffic.</dd>

        <dt>MMR</dt>
        <dd>Max-Mean Ratio. The ratio of the flow count (or traffic load) on the
        most heavily utilized link to the average flow count per link across all
        fabric links in the measurement path. MMR = 1.0 indicates perfect ECMP
        balance; MMR &gt; 1.0 quantifies imbalance that degrades effective
        fabric bandwidth.</dd>

      </dl>
    </section>

    <!-- ── Section 7: Fabric Topology Terms ──────────────────────── -->
    <section anchor="topology-terms" numbered="true" toc="default">
      <name>Fabric Topology and Infrastructure Terms</name>
      <t>
        The following terms define fabric topology architectures and
        infrastructure components referenced in the companion methodology
        documents.
      </t>
      <dl newline="true" spacing="normal">

        <dt>Clos / Fat-Tree Topology</dt>
        <dd>A multi-stage switch topology providing non-blocking or
        oversubscribed connectivity between all leaf-to-leaf pairs. In AI
        fabric deployments, a two-tier (leaf-spine) or three-tier
        (leaf-spine-superspine) Clos is standard. Full bisection bandwidth
        (1:1) is the target for training fabrics; 2:1 or 4:1 oversubscription
        may be acceptable for inference fabrics.</dd>

        <dt>Rail-Optimized Topology</dt>
        <dd>A topology in which the NIC ports of each server are distributed
        across multiple top-of-rack (ToR) switches (one NIC port per switch),
        such that intra-node collective traffic between adjacent servers
        traverses different physical paths. Rail-optimized designs minimize
        switch-to-switch traffic during ring AllReduce, maximizing effective
        BusBW. Requires ECMP-aware collective placement.</dd>

        <dt>Bisection Bandwidth</dt>
        <dd>The aggregate bandwidth across the minimum cut that divides the
        fabric into two equal halves. Non-blocking fabrics provide bisection
        bandwidth equal to half the total edge (server-facing) bandwidth.
        Bisection bandwidth limits worst-case all-to-all communication
        throughput.</dd>

        <dt>Oversubscription Ratio</dt>
        <dd>The ratio of total edge (server-facing) bandwidth to total bisection
        bandwidth in a Clos fabric. A 1:1 ratio is non-blocking; higher ratios
        (e.g., 2:1, 4:1) reduce fabric cost but may bottleneck all-to-all and
        AllReduce patterns when all server ports are active
        simultaneously.</dd>

        <dt>ToR Switch</dt>
        <dd>Top-of-Rack switch. The first-hop aggregation switch connecting
        accelerator servers in a rack to the spine layer of the fabric. In
        rail-optimized topologies, multiple ToR switches serve a single rack,
        with each server's NICs distributed across ToRs.</dd>

        <dt>Spine / Superspine</dt>
        <dd>Intermediate and top-layer switches in a multi-tier Clos fabric,
        providing inter-rack and inter-pod connectivity respectively. Spine
        switches aggregate multiple ToR switches; superspine switches aggregate
        multiple spine pods.</dd>

        <dt>NIC</dt>
        <dd>Network Interface Controller. The hardware device providing network
        connectivity for an accelerator host. AI fabric NICs support RDMA
        (RoCEv2 or UET), hardware offload for collective operations, and,
        optionally, GPU-Initiated Networking (GIN). NIC model and firmware
        version <bcp14>MUST</bcp14> be documented in all benchmark
        reports.</dd>

        <dt>Buffer Occupancy</dt>
        <dd>The instantaneous or time-averaged fill level of a switch port's
        packet buffer, expressed in bytes or as a fraction of total buffer
        capacity. Elevated sustained buffer occupancy indicates congestion.
        P99 buffer occupancy is a Fabric Health Indicator in the companion
        methodology documents.</dd>

        <dt>Zero-Impact Failover</dt>
        <dd>Sub-microsecond automatic path convergence upon a link or switch
        failure resulting in no measurable increase to JCT or TTFT.
        Zero-impact failover requires pre-programmed alternate paths and
        hardware-level fast reroute (FRR) with sub-microsecond detection, not
        relying on routing protocol convergence.</dd>

        <dt>Link Utilization</dt>
        <dd>The fraction of the nominal link capacity actually used for data
        transmission over a measurement interval, expressed as a percentage.
        Reported as mean, P95, and P99 per link. High asymmetric link
        utilization (low average but high peak) is characteristic of bursty AI
        inference traffic.</dd>

      </dl>
    </section>

    <!-- ── Section 8: Training-Specific Terms ────────────────────── -->
    <section anchor="training-terms" numbered="true" toc="default">
      <name>Training-Specific Terms</name>
      <t>
        The following terms are specific to AI training workload benchmarking
        and are used normatively in <xref target="TRAINING-BENCH-00"/>.
      </t>
      <dl newline="true" spacing="normal">

        <dt>JCT</dt>
        <dd>Job Completion Time. The wall-clock elapsed time from the start of
        a training job (or benchmark iteration) until all participating
        accelerators complete their work, inclusive of all forward pass,
        backward pass, and collective communication phases. JCT is the primary
        end-to-end training efficiency KPI.</dd>

        <dt>Roofline JCT</dt>
        <dd>The theoretical minimum JCT assuming perfect (zero-contention,
        zero-queuing) network behavior, calculated as:
        <artwork align="center" type="ascii-art"><![CDATA[
Roofline JCT = computation_time + serialization_delay
serialization_delay = message_size / link_rate
        ]]></artwork>
        Provides a reference for evaluating fabric overhead.</dd>

        <dt>JCT Ratio</dt>
        <dd>The ratio of measured JCT to Roofline JCT. A value of 1.0
        indicates no network-induced overhead. Values &gt; 1.0 quantify
        fabric inefficiency:
        <artwork align="center" type="ascii-art"><![CDATA[
JCT Ratio = JCT_measured / JCT_roofline
        ]]></artwork>
        The JCT Ratio is the primary comparative metric for AI training fabric
        benchmarking.</dd>

        <dt>Gradient Synchronization</dt>
        <dd>The AllReduce collective operation performed after the backward pass
        of each training step to sum the locally computed gradients across all
        data-parallel replicas. Gradient synchronization is the dominant
        communication event in data-parallel training, occurring once per
        training step per layer.</dd>

        <dt>Step Time</dt>
        <dd>The wall-clock duration of a single training iteration (one forward
        pass + one backward pass + gradient synchronization + optimizer step).
        Step time = computation time + communication time, where the
        communication time is dominated by the AllReduce collective.</dd>

        <dt>Soak Test</dt>
        <dd>A sustained-load test run for an extended period (minimum 24 hours
        for stability evaluation) at a defined offered load fraction (e.g., 70%
        or 90% of maximum throughput). Soak tests detect buffer leaks, ECMP
        imbalance drift, PFC storm initiation, and long-tail error accumulation
        not visible in short-duration tests.</dd>

      </dl>
    </section>

    <!-- ── Section 9: Inference-Specific Terms ────────────────────── -->
    <section anchor="inference-terms" numbered="true" toc="default">
      <name>Inference-Specific Terms</name>
      <t>
        The following terms are specific to AI inference serving workload
        benchmarking and are used normatively in
        <xref target="INFERENCE-BENCH-00"/>.
      </t>
      <dl newline="true" spacing="normal">

        <dt>TTFT</dt>
        <dd>Time to First Token. The elapsed time from receipt of an inference
        request by the serving system to emission of the first output token.
        TTFT encompasses prompt processing (prefill), KV cache generation,
        optional KV cache transfer (in disaggregated architectures), and the
        initial decode step. Interactive serving target: TTFT &lt; 500 ms
        at P99.</dd>

        <dt>ITL</dt>
        <dd>Inter-Token Latency. The elapsed time between successive output
        tokens during the autoregressive decode phase. ITL is measured at P50,
        P95, P99, and P99.9 to characterize tail latency behavior. Interactive
        serving target: ITL &lt; 50 ms at P99.</dd>

        <dt>TPS</dt>
        <dd>Tokens Per Second. Aggregate throughput of the inference serving
        system, measured as the total number of output tokens generated per
        second across all concurrent requests. Reported separately for
        input-side (prefill) TPS and output-side (decode) TPS.</dd>

        <dt>KV Cache</dt>
        <dd>Key-Value Cache. The intermediate attention state (key and value
        projection matrices from the multi-head attention layers) computed
        during the prefill phase and reused during each decode step to avoid
        redundant recomputation. KV cache size scales with: number of layers
        &times; number of attention heads &times; head dimension &times;
        sequence length &times; numerical precision. The attention head
        configuration <bcp14>MUST</bcp14> be reported in all benchmark
        results.</dd>

        <dt>Prefill Phase</dt>
        <dd>The compute-bound phase of LLM inference in which the entire input
        prompt is processed in parallel to generate the KV cache and the first
        output token. Characterized by high arithmetic intensity (200&ndash;400
        ops/byte), high accelerator utilization (90&ndash;95%), and large
        activation tensors. Prefill latency dominates TTFT for long
        prompts.</dd>

        <dt>Decode Phase</dt>
        <dd>The memory-bandwidth-bound phase of LLM inference in which output
        tokens are generated autoregressively, one token per forward pass, by
        reading the KV cache. Characterized by low arithmetic intensity
        (60&ndash;80 ops/byte), lower accelerator utilization (20&ndash;40%),
        and memory-bandwidth-limited KV cache reads. Decode throughput limits
        TPS.</dd>

        <dt>Disaggregated Serving</dt>
        <dd>An inference serving architecture in which the prefill phase and
        decode phase are executed on physically separate groups of accelerators
        (workers), connected by a network fabric. Disaggregated serving allows
        independent scaling of prefill and decode resources (xPyD) but
        introduces KV cache transfer as a fabric-critical data
        movement.</dd>

        <dt>xPyD Ratio</dt>
        <dd>The allocation ratio of x prefill workers to y decode workers in a
        disaggregated serving cluster. Notation example: 3P9D denotes 3 prefill
        nodes and 9 decode nodes. The optimal xPyD ratio depends on model
        size, prompt length distribution, output length distribution, and
        TTFT/ITL SLO targets.</dd>

        <dt>Continuous Batching</dt>
        <dd>A dynamic inference scheduling technique that inserts new requests
        into an active decode batch as slots become available (without waiting
        for the current batch to complete), improving accelerator utilization
        compared to static batching. Continuous batching generates variable
        batch sizes that affect fabric traffic burstiness.</dd>

        <dt>PagedAttention</dt>
        <dd>A KV cache memory management technique storing attention keys and
        values in fixed-size, non-contiguous virtual pages (typically
        16&ndash;64&nbsp;KB), inspired by OS virtual memory management.
        PagedAttention reduces memory fragmentation and enables efficient KV
        cache sharing across requests with common prefixes.</dd>

        <dt>Prefix Caching</dt>
        <dd>Reuse of previously computed KV cache segments for inference
        requests sharing a common prompt prefix (e.g., a fixed system prompt),
        eliminating redundant prefill computation. Prefix cache hit rate is a
        secondary KPI for inference serving efficiency.</dd>

        <dt>Normal Dispatch</dt>
        <dd>An AllToAll MoE dispatch communication mode optimized for the
        prefill phase. Payload sizes are variable (depending on token-to-expert
        routing), generating dynamic tensor shapes incompatible with static
        graph capture. Normal Dispatch maximizes throughput for large batches
        at the cost of higher per-dispatch latency.</dd>

        <dt>Low-Latency Dispatch</dt>
        <dd>An AllToAll MoE dispatch communication mode optimized for the
        decode phase. Payload sizes are padded to fixed maximum dimensions
        (compatible with static graph capture), enabling lower kernel-launch
        overhead at the cost of slight bandwidth inefficiency. Target latency:
        &lt; 200&nbsp;&micro;s per dispatch round trip.</dd>

        <dt>SLO</dt>
        <dd>Service Level Objective. A quantitative target for an inference
        serving KPI, established between the service provider and consumers.
        AI inference SLOs typically specify maximum TTFT (e.g., &lt; 500 ms
        P99) and maximum ITL (e.g., &lt; 50 ms P99) under a specified request
        arrival rate.</dd>

        <dt>Speculative Decoding</dt>
        <dd>An inference acceleration technique using a small draft model to
        generate candidate token sequences that are verified in parallel by the
        target model. Speculative decoding reduces effective ITL but generates
        bursty, variable-length KV cache traffic; this workload pattern is
        noted as a future benchmarking area not fully specified in the current
        companion documents.</dd>

      </dl>
    </section>

    <!-- ── Section 10: KPI Classification Terms ──────────────────── -->
    <section anchor="kpi-terms" numbered="true" toc="default">
      <name>KPI Classification Terms</name>
      <t>
        The following terms define the three-tier KPI taxonomy used across both
        companion methodology documents.
      </t>
      <dl newline="true" spacing="normal">

        <dt>Primary KPI</dt>
        <dd>A top-level performance indicator directly representing end-user
        experience or training efficiency. In training: JCT Ratio and BusBW.
        In inference: TTFT and ITL. Primary KPIs are the principal reporting
        metric and the basis for comparative benchmarking across DUT
        implementations.</dd>

        <dt>Secondary KPI</dt>
        <dd>A fabric-level performance indicator providing mechanistic
        explanation for primary KPI values. Examples: collective operation
        throughput (BusBW), KV cache transfer goodput, AllToAll dispatch
        latency, ECMP imbalance (MMR), and link utilization. Secondary KPIs
        enable root-cause analysis of Primary KPI deviations.</dd>

        <dt>Fabric Health Indicator (FHI)</dt>
        <dd>An operational metric characterizing fabric stability and anomaly
        conditions rather than peak performance. FHIs include: PFC event rate,
        PFC storm occurrence, ECN marking ratio, packet loss rate, buffer
        occupancy (P99), and retransmission rate. FHIs <bcp14>SHOULD</bcp14>
        be continuously monitored and reported throughout all test
        categories.</dd>

        <dt>Goodput</dt>
        <dd>The application-useful data delivered per unit time, excluding
        retransmitted packets, protocol overhead, and padding. Goodput may
        differ significantly from raw throughput during congestion events;
        both <bcp14>SHOULD</bcp14> be reported in benchmarking results.</dd>

        <dt>Zero Packet Loss</dt>
        <dd>A test acceptance criterion requiring that no packets are dropped by
        the DUT during the measurement interval. For RoCEv2 and UET transports,
        zero packet loss is the target operating condition. The binary search
        procedure in the companion methodology documents determines the maximum
        offered load satisfying this criterion.</dd>

      </dl>
    </section>

    <!-- ── Section 11: Referenced Standards Abbreviations ────────── -->
    <section anchor="std-abbrev" numbered="true" toc="default">
      <name>Referenced Standards Abbreviations</name>
      <t>
        The following abbreviations refer to normative and informative IETF
        documents referenced throughout this document and the companion
        methodology documents.
      </t>
      <dl newline="true" spacing="normal">

        <dt>RFC 1242</dt>
        <dd>"Benchmarking Terminology for Network Interconnect Devices"
        (Bradner, 1991). Defines foundational benchmarking terms (throughput,
        latency, frame loss rate, back-to-back frames). The baseline
        terminology reference for BMWG work. Where terms in this document
        overlap with RFC 1242 definitions, the AI fabric context definitions
        herein take precedence.</dd>

        <dt>RFC 2544</dt>
        <dd>"Benchmarking Methodology for Network Interconnect Devices"
        (Bradner &amp; McQuaid, 1999). Defines test methodologies for
        throughput, latency, frame loss rate, and back-to-back measurements.
        The AI fabric methodology documents extend RFC&nbsp;2544 procedures for
        AI-specific traffic patterns and test durations.</dd>

        <dt>RFC 8238</dt>
        <dd>"Data Center Benchmarking Terminology" (Bitar et al., 2017).
        Extends RFC&nbsp;1242 with data center-relevant terms including
        forwarding table scaling, congestion, and VM/SDN. Incast, ECN, and
        buffer occupancy concepts in this document align with RFC&nbsp;8238
        definitions.</dd>

        <dt>RFC 8239</dt>
        <dd>"Data Center Benchmarking Methodology" (Bitar et al., 2017).
        Defines test methodologies for data center network functions including
        incast, ECN marking, and lossless behavior. The AI fabric companion
        methodology documents extend RFC&nbsp;8239 for distributed AI
        collective traffic patterns.</dd>

        <dt>RFC 2119 / RFC 8174</dt>
        <dd>"Key words for use in RFCs to Indicate Requirement Levels"
        (Bradner, 1997; Leiba, 2017). Define the normative requirement
        language: MUST, MUST NOT, REQUIRED, SHALL, SHALL NOT, SHOULD, SHOULD
        NOT, RECOMMENDED, MAY, and OPTIONAL. RFC&nbsp;8174 clarifies that these
        terms are normative only when in uppercase; lowercase uses are not
        normative.</dd>

      </dl>
    </section>

    <!-- ── Section 12: Security Considerations ───────────────────── -->
    <section anchor="security" numbered="true" toc="default">
      <name>Security Considerations</name>
      <t>
        This document defines terminology for use in controlled laboratory
        benchmarking environments. As such it does not define protocols,
        algorithms, or operational procedures that introduce security
        vulnerabilities.
      </t>
      <t>
        Benchmarking activities <bcp14>SHOULD</bcp14> be conducted in isolated
        test networks that are not connected to production infrastructure. AI
        fabric benchmarking generates substantial traffic volumes (multi-terabit
        aggregate); inadvertent exposure to operational networks could cause
        service disruption.
      </t>
      <t>
        Published benchmarking results <bcp14>MUST</bcp14> accurately represent
        the tested configuration including DUT hardware and firmware versions,
        transport configuration, and topology. Selective reporting of favorable
        configurations while omitting constraints may mislead procurement
        decisions.
      </t>
      <t>
        Refer to <xref target="RFC2544"/> Section&nbsp;6 and the companion
        methodology documents for additional security considerations related to
        test traffic isolation and result integrity.
      </t>
    </section>

    <!-- ── Section 13: IANA Considerations ───────────────────────── -->
    <section anchor="iana" numbered="true" toc="default">
      <name>IANA Considerations</name>
      <t>This document has no IANA actions.</t>
      <t>
        The companion document <xref target="INFERENCE-BENCH-00"/> references
        UDP port 4793 as the destination port for UET traffic. The authors note
        that IANA assignment of this port number for UET <bcp14>SHOULD</bcp14>
        be verified against the IANA Service Name and Transport Protocol Port
        Number Registry prior to publication of that document. The UEC
        Specification 1.0 assignment status <bcp14>SHOULD</bcp14> be confirmed
        by the document authors.
      </t>
    </section>

  </middle>

  <!-- ============================================================ -->
  <!--  BACK MATTER                                                  -->
  <!-- ============================================================ -->
  <back>

    <!-- References -->
    <references>
      <name>References</name>

      <references anchor="normative-refs">
        <name>Normative References</name>

        <reference anchor="RFC2119" target="https://www.rfc-editor.org/rfc/rfc2119">
          <front>
            <title>Key words for use in RFCs to Indicate Requirement Levels</title>
            <author initials="S." surname="Bradner" fullname="S. Bradner"/>
            <date month="March" year="1997"/>
          </front>
          <seriesInfo name="BCP" value="14"/>
          <seriesInfo name="RFC" value="2119"/>
          <seriesInfo name="DOI" value="10.17487/RFC2119"/>
        </reference>

        <reference anchor="RFC8174" target="https://www.rfc-editor.org/rfc/rfc8174">
          <front>
            <title>Ambiguity of Uppercase vs Lowercase in RFC 2119 Key Words</title>
            <author initials="B." surname="Leiba" fullname="B. Leiba"/>
            <date month="May" year="2017"/>
          </front>
          <seriesInfo name="BCP" value="14"/>
          <seriesInfo name="RFC" value="8174"/>
          <seriesInfo name="DOI" value="10.17487/RFC8174"/>
        </reference>

        <reference anchor="RFC1242" target="https://www.rfc-editor.org/rfc/rfc1242">
          <front>
            <title>Benchmarking Terminology for Network Interconnect Devices</title>
            <author initials="S." surname="Bradner" fullname="S. Bradner"/>
            <date month="July" year="1991"/>
          </front>
          <seriesInfo name="RFC" value="1242"/>
          <seriesInfo name="DOI" value="10.17487/RFC1242"/>
        </reference>

        <reference anchor="RFC2544" target="https://www.rfc-editor.org/rfc/rfc2544">
          <front>
            <title>Benchmarking Methodology for Network Interconnect Devices</title>
            <author initials="S." surname="Bradner" fullname="S. Bradner"/>
            <author initials="J." surname="McQuaid" fullname="J. McQuaid"/>
            <date month="March" year="1999"/>
          </front>
          <seriesInfo name="RFC" value="2544"/>
          <seriesInfo name="DOI" value="10.17487/RFC2544"/>
        </reference>

      </references>

      <references anchor="informative-refs">
        <name>Informative References</name>

        <reference anchor="RFC8238" target="https://www.rfc-editor.org/rfc/rfc8238">
          <front>
            <title>Data Center Benchmarking Terminology</title>
            <author initials="L." surname="Avramov" fullname="L. Avramov"/>
            <author initials="J." surname="Rapp" fullname="J. Rapp"/>
            <date month="August" year="2017"/>
          </front>
          <seriesInfo name="RFC" value="8238"/>
          <seriesInfo name="DOI" value="10.17487/RFC8238"/>
        </reference>

        <reference anchor="RFC8239" target="https://www.rfc-editor.org/rfc/rfc8239">
          <front>
            <title>Data Center Benchmarking Methodology</title>
            <author initials="L." surname="Avramov" fullname="L. Avramov"/>
            <author initials="J." surname="Rapp" fullname="J. Rapp"/>
            <date month="August" year="2017"/>
          </front>
          <seriesInfo name="RFC" value="8239"/>
          <seriesInfo name="DOI" value="10.17487/RFC8239"/>
        </reference>

        <reference anchor="RFC3168" target="https://www.rfc-editor.org/rfc/rfc3168">
          <front>
            <title>The Addition of Explicit Congestion Notification (ECN) to IP</title>
            <author initials="K." surname="Ramakrishnan" fullname="K. Ramakrishnan"/>
            <author initials="S." surname="Floyd" fullname="S. Floyd"/>
            <author initials="D." surname="Black" fullname="D. Black"/>
            <date month="September" year="2001"/>
          </front>
          <seriesInfo name="RFC" value="3168"/>
          <seriesInfo name="DOI" value="10.17487/RFC3168"/>
        </reference>

        <reference anchor="IBTA-ROCE" target="https://www.infinibandta.org">
          <front>
            <title>InfiniBand Architecture Specification, Annex 16: RoCE</title>
            <author>
              <organization>InfiniBand Trade Association</organization>
            </author>
            <date year="2010"/>
          </front>
        </reference>

        <reference anchor="UEC-SPEC-1.0" target="https://ultraethernet.org">
          <front>
            <title>Ultra Ethernet Specification 1.0</title>
            <author>
              <organization>Ultra Ethernet Consortium</organization>
            </author>
            <date year="2024"/>
          </front>
        </reference>

        <reference anchor="TRAINING-BENCH-00">
          <front>
            <title>Benchmarking Methodology for AI Training Network Fabrics</title>
            <author initials="F." surname="Calabria" fullname="F. Calabria"/>
            <author initials="C." surname="Pignataro" fullname="C. Pignataro"/>
            <date month="February" year="2026"/>
          </front>
          <seriesInfo name="Internet-Draft"
                      value="draft-bmwg-ai-fabric-training-bench-00"/>
        </reference>

        <reference anchor="INFERENCE-BENCH-00">
          <front>
            <title>Benchmarking Methodology for AI Inference Serving Network Fabrics</title>
            <author initials="F." surname="Calabria" fullname="F. Calabria"/>
            <author initials="C." surname="Pignataro" fullname="C. Pignataro"/>
            <date month="February" year="2026"/>
          </front>
          <seriesInfo name="Internet-Draft"
                      value="draft-bmwg-ai-fabric-inference-bench-00"/>
        </reference>

        <reference anchor="charter-ietf-bmwg"
                   target="https://datatracker.ietf.org/group/bmwg/about/">
          <front>
            <title>Benchmarking Methodology Working Group Charter</title>
            <author>
              <organization>IETF</organization>
            </author>
            <date year="2024"/>
          </front>
        </reference>

      </references>
    </references>

    <!-- ── Appendix A: Term Cross-Reference ──────────────────────── -->
    <section anchor="appendix-xref" numbered="false" toc="default">
      <name>Appendix A: Term Cross-Reference to Companion Documents</name>
      <t>
        The following table identifies which terms from this document are used
        in each companion methodology document.
      </t>
      <table anchor="tbl-xref" align="left">
        <name>Term Cross-Reference to Companion Documents</name>
        <thead>
          <tr>
            <th>Term Category</th>
            <th>Used in Training Bench</th>
            <th>Used in Inference Bench</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>General Benchmarking Terms (Section 2)</td>
            <td>All terms</td>
            <td>All terms</td>
          </tr>
          <tr>
            <td>Collective Communication (Section 3)</td>
            <td>AllReduce, AllGather, ReduceScatter, AllToAll, BusBW, CCL, Ring Algorithm, BSP, SPMD</td>
            <td>AllToAll, BusBW</td>
          </tr>
          <tr>
            <td>Parallelism Strategies (Section 4)</td>
            <td>DP, TP, PP, EP, MoE, ZeRO</td>
            <td>EP, MoE, DP Attention</td>
          </tr>
          <tr>
            <td>RDMA / RoCEv2 (Section 5.1)</td>
            <td>RDMA, RoCEv2, QP, RC mode, RDMA Verb</td>
            <td>RDMA, RoCEv2, QP, RC mode, GIN, KVCXL</td>
          </tr>
          <tr>
            <td>UET Terms (Section 5.2)</td>
            <td>UET, PDC, ROD, RUD, RUDI, UUD, LLR, Packet Trimming, CBFC, UEC Profile, Entropy Value</td>
            <td>UET, RUD, GIN</td>
          </tr>
          <tr>
            <td>Congestion Control (Section 6)</td>
            <td>PFC, PFC Storm, PFC Deadlock, ECN, DCQCN, ECN Marking Ratio, Incast, Incast Ratio, Packet Spray, DLB/Flowlet, ECMP, MMR</td>
            <td>PFC, ECN, DCQCN, Incast, Packet Spray, ECMP</td>
          </tr>
          <tr>
            <td>Fabric Topology (Section 7)</td>
            <td>Clos, Rail-Optimized, Bisection BW, Oversubscription, ToR, Spine, NIC, Buffer Occupancy, Zero-Impact Failover, Link Utilization</td>
            <td>Clos, Bisection BW, ToR, NIC, Buffer Occupancy, Link Utilization</td>
          </tr>
          <tr>
            <td>Training-Specific (Section 8)</td>
            <td>JCT, Roofline JCT, JCT Ratio, Gradient Sync, Step Time, Soak Test</td>
            <td>Soak Test</td>
          </tr>
          <tr>
            <td>Inference-Specific (Section 9)</td>
            <td>—</td>
            <td>TTFT, ITL, TPS, KV Cache, Prefill, Decode, Disaggregated Serving, xPyD, Continuous Batching, PagedAttention, Prefix Caching, Normal/Low-Latency Dispatch, SLO</td>
          </tr>
          <tr>
            <td>KPI Classification (Section 10)</td>
            <td>Primary KPI (JCT Ratio, BusBW), Secondary KPI, FHI, Goodput, Zero Packet Loss</td>
            <td>Primary KPI (TTFT, ITL), Secondary KPI, FHI, Goodput, Zero Packet Loss</td>
          </tr>
        </tbody>
      </table>
    </section>

    <!-- ── Appendix B: Term Taxonomy Summary ─────────────────────── -->
    <section anchor="appendix-taxonomy" numbered="false" toc="default">
      <name>Appendix B: Term Taxonomy Summary</name>
      <t>
        The following table provides a concise summary of all defined terms
        organized by category, with the section reference for the full
        definition.
      </t>
      <table anchor="tbl-taxonomy" align="left">
        <name>Complete Term Taxonomy</name>
        <thead>
          <tr><th>Section</th><th>Term(s)</th><th>Category</th></tr>
        </thead>
        <tbody>
          <tr>
            <td>2</td>
            <td>DUT, SUT, RT, JFI, Offered Load, Trial Duration, Warmup Period, Binary Search, Percentile Latency, AI Fabric</td>
            <td>General Benchmarking</td>
          </tr>
          <tr>
            <td>3</td>
            <td>Collective Operation, AllReduce, AllGather, ReduceScatter, AllToAll, Ring Algorithm, BusBW, CCL, SPMD, BSP</td>
            <td>Collective Communication</td>
          </tr>
          <tr>
            <td>4</td>
            <td>Data Parallelism, Tensor Parallelism, Pipeline Parallelism, Expert Parallelism, MoE, DP Attention, ZeRO</td>
            <td>Parallelism Strategies</td>
          </tr>
          <tr>
            <td>5.1</td>
            <td>RDMA, RoCEv2, QP, Reliable Connected (RC), RDMA Verb, UET, PDC, ROD</td>
            <td>Transport — RDMA / RoCEv2</td>
          </tr>
          <tr>
            <td>5.2</td>
            <td>RUD, RUDI, UUD, UEC Profile, LLR, Packet Trimming, CBFC, Entropy Value, GIN, KVCXL</td>
            <td>Transport — UET</td>
          </tr>
          <tr>
            <td>6</td>
            <td>PFC, PFC Storm, PFC Deadlock, ECN, DCQCN, ECN Marking Ratio, Incast, Incast Ratio, Packet Spray, DLB/Flowlet, ECMP, MMR</td>
            <td>Congestion Control</td>
          </tr>
          <tr>
            <td>7</td>
            <td>Clos/Fat-Tree, Rail-Optimized, Bisection Bandwidth, Oversubscription Ratio, ToR Switch, Spine/Superspine, NIC, Buffer Occupancy, Zero-Impact Failover, Link Utilization</td>
            <td>Fabric Topology</td>
          </tr>
          <tr>
            <td>8</td>
            <td>JCT, Roofline JCT, JCT Ratio, Gradient Synchronization, Step Time, Soak Test</td>
            <td>Training-Specific</td>
          </tr>
          <tr>
            <td>9</td>
            <td>TTFT, ITL, TPS, KV Cache, Prefill Phase, Decode Phase, Disaggregated Serving, xPyD Ratio, Continuous Batching, PagedAttention, Prefix Caching, Normal Dispatch, Low-Latency Dispatch, SLO, Speculative Decoding</td>
            <td>Inference-Specific</td>
          </tr>
          <tr>
            <td>10</td>
            <td>Primary KPI, Secondary KPI, Fabric Health Indicator, Goodput, Zero Packet Loss</td>
            <td>KPI Classification</td>
          </tr>
          <tr>
            <td>11</td>
            <td>RFC 1242, RFC 2544, RFC 8238, RFC 8239, RFC 2119/8174</td>
            <td>Referenced Standards</td>
          </tr>
        </tbody>
      </table>
    </section>

  </back>
</rfc>
